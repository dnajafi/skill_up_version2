import React from 'react';

const Dashboard = ({ 
	currentUser
}) => {

	console.log('&&&&&&&&&&&&&&&&&&&&&&&&&&&&&');
	console.log(currentUser);

	return (
		<div>
			<p>yoyoyo</p>
  	</div>
	);
};

export default Dashboard;